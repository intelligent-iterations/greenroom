{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM for WebSocket and the audio types the transport names. The protocol
    // module itself is platform-free and runs anywhere.
    "lib": ["ES2023", "DOM"],
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true
  },
  "include": ["src"],
  "exclude": ["src/__tests__"]
}
