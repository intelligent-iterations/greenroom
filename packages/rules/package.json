{
  "name": "@greenroom/rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project greenroom-test 'vitest run'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/node": "^22.20.1",
    "firebase": "^12.18.0",
    "firebase-tools": "15.29.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
