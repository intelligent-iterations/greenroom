**What this changes, and why**

**What you verified, and how**

**What you did not verify.** If this touches audio, hardware or a vendor and you
could not run it against the real thing, say so here and add it to the README's
"not verified" list. That list is the most valuable thing in the repository.

- [ ] `pnpm test` and `pnpm typecheck` pass
- [ ] New checks have a test proving they fire and one proving they stay quiet
- [ ] `PROMPT_VERSION` bumped if compiled prompt output changed
